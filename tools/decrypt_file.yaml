identity:
  author: "sawyer-shi"
  name: "decrypt_file"
  label:
    en_US: "Decrypt File Normal"
    zh_Hans: "文件通用解密"
description:
  human:
    en_US: "Decrypt the files xxx.pdf.locked, xxx.mp4.enc, and xxx.xlsx.secret ... into their original formats (e.g., xxx.pdf.locked → xxx.pdf...)."
    zh_Hans: "将xxx.pdf.locked、xxx.mp4.enc、xxx.xlsx.secret...文件解密为原始文件（如：xxx.pdf.locked -> xxx.pdf...）。"
  llm: "Decrypt the files xxx.pdf.locked, xxx.mp4.enc, and xxx.xlsx.secret ... into their original formats (e.g., xxx.pdf.locked → xxx.pdf...)."
class_name: DecryptFile
parameters:
  - name: file
    label:
      en_US: "Encrypted File"
      zh_Hans: "加密文件"
    type: file
    required: true
    form: llm
    human_description:
      en_US: "The encrypted file to decrypt."
      zh_Hans: "要解密的加密文件。"
    llm_description: "The encrypted file to decrypt."
  - name: key
    label:
      en_US: "Decryption Key"
      zh_Hans: "解密秘钥"
    type: string
    required: true
    form: llm
    human_description:
      en_US: "The decryption key used during encryption."
      zh_Hans: "加密时使用的解密秘钥。"
    llm_description: "The decryption key used during encryption."
extra:
  python:
    source: tools/decrypt_file.py
