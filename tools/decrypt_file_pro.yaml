identity:
  author: "sawyer-shi"
  name: "decrypt_file_pro"
  label:
    en_US: "Decrypt File Pro"
    zh_Hans: "文件高级解密"
description:
  human:
    en_US: "Decrypt the files xxx.locked, xxx.enc, and xxx.secret ... into their original formats (e.g., test.locked → test.mp4、test.pdf、test.zip、test.docx...)."
    zh_Hans: "将xxx.locked、xxx.enc、xxx.secret...文件解密为原始文件（如：test.locked -> test.mp4、test.pdf、test.zip、test.docx...）。"
  llm: "Decrypt the files xxx.locked, xxx.enc, and xxx.secret ... into their original formats (e.g., test.locked → test.mp4、test.pdf、test.zip、test.docx...)."
class_name: DecryptFilePro
parameters:
  - name: file
    label:
      en_US: "Encrypted File"
      zh_Hans: "加密文件"
    type: file
    required: true
    form: llm
    human_description:
      en_US: "The encrypted file to decrypt."
      zh_Hans: "要解密的加密文件。"
    llm_description: "The encrypted file to decrypt."
  - name: key
    label:
      en_US: "Decryption Key"
      zh_Hans: "解密秘钥"
    type: string
    required: true
    form: llm
    human_description:
      en_US: "The decryption key used during encryption."
      zh_Hans: "加密时使用的解密秘钥。"
    llm_description: "The decryption key used during encryption."
extra:
  python:
    source: tools/decrypt_file_pro.py
